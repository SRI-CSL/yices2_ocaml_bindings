; EXPECT: unsat
(set-logic QF_S)
(declare-const x String)
(declare-const y String)
(declare-const z String)
(assert (str.in_re x (re.range "a" "c")))
(assert (str.in_re z (re.++ (re.range "a" "c") (str.to_re "d"))))
(assert (= z (str.++ x y)))
(assert (not (= y "d")))
(check-sat)
